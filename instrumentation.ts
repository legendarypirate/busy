/**
 * Next.js instrumentation hook (optional). Keep file present so Turbopack dev cache
 * does not reference a deleted module. Env / DB: see `src/lib/prisma.ts`.
 */
export function register() {
  /* intentionally empty */
}
