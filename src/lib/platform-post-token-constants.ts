/** Primary hidden field for signed post-token (Server Actions / multipart). */
export const PLATFORM_POST_TOKEN_FIELD = "bni_platform_post_token";

/** Legacy name — still accepted when verifying. */
export const PLATFORM_TRIP_SAVE_TOKEN_FIELD = "bni_platform_trip_save_token";
